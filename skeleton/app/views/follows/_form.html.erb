<% if current_user.follows?(user) %>
  <% difs = "followed" %>
<% else %>
  <% difs = "unfollowed" %>
<% end %>
<button class="follow-toggle" data-user-id="<%= user.id %>" data-initial-follow-state="<%= difs %>" type="button" name="button">Hello</button>
